<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>{% block title %}Welcome!{% endblock %}</title>
    <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text><text y=%221.3em%22 x=%220.2em%22 font-size=%2276%22 fill=%22%23fff%22>sf</text></svg>">

    <!-- POLICE ROBOTO MONO-->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:ital,wght@0,100;0,200;0,300;0,400;0,500;0,600;0,700;0,800;0,900;1,100;1,200;1,300;1,400;1,500;1,600;1,700;1,800;1,900&family=Roboto+Mono:ital,wght@0,100..700;1,100..700&family=Rock+Salt&display=swap" rel="stylesheet">

    <!-- Lien de Bootstrap -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">

    <!-- Lien CSS -->
    <link rel="stylesheet" href="{{asset('assets/css/style.css') }} ">
</head>

    <body>

        <header>
            <nav>
                <ul class="navbar">
                    <li>
                        <a>
                            <img class="logo" src="{{ asset('assets/images/LOGO%20REVERSE%20BLACK.png') }}" alt="logo">
                        </a>
                    </li>
                    <li><a href="{{ path('homepage') }}">Accueil</a></li>
                    <li><a href="{{ path('list_tracks') }}" >Releases</a></li>
                    <li><a href="{{ path('list_artists') }}">Artistes</a></li>
                    <li><a href="{{ path('contact') }}">Contact</a></li>
                </ul>
            </nav>
        </header>

        {% for message in app.flashes('success') %}
            <section>
                <p style="background-color: green; color: white">{{ message }}</p>
            </section>
        {% endfor %}

        {% for message in app.flashes('error') %}
            <section>
                <p style="background-color: red; color: white">{{ message }}</p>
            </section>
        {% endfor %}


            {% block body %}
            {% endblock %}


        <footer>
            <nav>
                <ul class="navbarFooter">
                    <li>
                        <a>
                            <img class="logo" src="{{ asset('assets/images/LOGO%20REVERSE%20BLACK.png') }}" alt="logo">
                        </a>
                    </li>
                    <li><a href="{{ path('homepage') }}">Accueil</a></li>
                    <li><a href="{{ path('list_tracks') }}">Releases</a></li>
                    <li><a href="{{ path('list_artists') }}">Artistes</a></li>
                    <li><a href="{{ path('contact') }}">Contact</a></li>
                    <li><a>Listen on</a>
                        <ul class="navbarPicto">
                            <li><img class="picto"  src="{{ asset('assets/images/youtube_picto.png')}}" style="width: 35px"></li>
                            <li><img class="picto"  src="{{ asset('assets/images/instagram_picto.png')}}"></li>
                            <li><img class="picto"  src="{{ asset('assets/images/soundcloud_picto.png')}}"></li>
                            <li><img class="picto"  src="{{ asset('assets/images/spotify_picto.png')}}" style="width: 35px"></li>
                        </ul>
                    </li>
                </ul>
            </nav>
        </footer>

    <!-- Lien JS-->

        <script src="{{ asset('assets/js/carousel.js') }}"></script>

    </body>
</html>
