{% extends 'admin/base.html.twig' %}
{% block body %}
    <section class="container my-5">
        <div class="d-flex justify-content-center">
            <div class="row align-items-center w-75">

                <!-- Colonne des informations -->
                <div class="col-md-8">
                    <p><strong>style_id :</strong> {{ style.id }}</p>
                    <p><strong>Nom du style :</strong> {{ style.name }}</p>
                    <p><strong>Date de création :</strong> {{ style.createdAt | date('d-m-Y') }}</p>
                    <p><strong>Date de modification :</strong> {{ style.updatedAt | date('d-m-Y') }}</p>

                </div>
            </div>
        </div>
    </section>
{% endblock body %}